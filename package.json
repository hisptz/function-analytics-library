{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"

  },
  "scripts": {
    "app": "yarn workspace app",
    "lib": "yarn workspace lib",
    "start": "yarn app start",
    "test": "yarn lib test"
  }
}
